#include "main_00.h"

unsigned ACTIVATE(Location**,Cell*,unsigned,unsigned,unsigned,char,char,double);
	/*args:
		Global Grid,
		Active List,
		row,
		col,
		active count,
		parent code,
		vent code,
		residual
	*/
